# ═══════════════════════════════════════════════════════════════
# SentinelX — Secrets
# Stores sensitive data like passwords (base64 encoded)
#
# To encode a value:  echo -n "your-value" | base64
# To decode a value:  echo "encoded-value" | base64 --decode
# ═══════════════════════════════════════════════════════════════
apiVersion: v1
kind: Secret
metadata:
  name: sentinelx-secrets
  namespace: sentinelx
type: Opaque
data:
  # "sentinelx-change-me-in-production" in base64
  JWT_SECRET_KEY: c2VudGluZWx4LWNoYW5nZS1tZS1pbi1wcm9kdWN0aW9u

  # Empty — replace with your AlienVault OTX API key
  OTX_API_KEY: ""

  # "SentinelX@2024" in base64
  ADMIN_PASSWORD: U2VudGluZWxYQDIwMjQ=

  # "analyst123" in base64
  ANALYST_PASSWORD: YW5hbHlzdDEyMw==
